# E13 · 下一任是谁 —— 双平台发布文案包

**成片**：`E13-下一任是谁合集\video\E13.mp4`（Step 5B 合并正片：E13A+E13B，30.198s，1056×1920 hevc+aac，统一 BGM 0.25 音量垫底）
**封面**：`E13-下一任是谁合集\images\E13_幕沛灵银月_闺阁同框B_下_双图直传.png`（**用户指定（2026-09-20）**：B 段双人参考图，65535 双图直传 2k 原图；构图为手机黑屏扣桌、两女同框，图内不含男友。原成片 9.5s 抽帧候选已移至 `任务记录\未采用封面\`，双平台同一张）
**角色**：幕沛灵（红宫装，画面左）· 银月（敦煌风裙，画面右）——系列第二次双女主同框（首次为 E12 紫灵+凌玉灵）
**热点**：抖音热搜「发视频解锁你的下一任对象」771 万
**称呼（用户锁死）**：幕沛灵全片 4 处称「公子」；银月全片 5 处称「主人」，已逐句核对
**互动（用户书面批准 L3）**：B 段 8s 前后，镜头外男友的手+前臂+玄色袖口自画面下缘入画，两指轻捏幕沛灵左颊后退出；已按 interaction-playbook §10 抽 27 帧核验——全程无第二人的脸/头/躯干。偏差（如实记录）：手实际在画约 7.9–8.8s（≈1s，规格 ≤0.6s）、峰值占比约 1/4–1/3（规格 ≤1/5）；§10 强制重出条件未触发，裁定接受

---

## 一、B 站（账号：bilibili_main／韩老魔的宫装女友）

- **分区**：`--tid 47`（动画·短片/同人）
- **版权**：自制 + 禁止转载
- **创作声明**：**含AI生成内容**（投稿时用 `--submit web --extra-fields` 一次带入；回读 `decl_id=1` 校验）
- **标题**（≤80 字）：
  `热搜说发视频能解锁下一任对象，两位女友当场审问起我来｜韩老魔的宫装女友13`
- **简介**：
  `热搜上有个梗：发视频能解锁你的下一任对象。银月举着手机凑到眼前献宝，幕沛灵伸手就把屏幕按了下去："公子也信这个？"两个人你一句"主人方才盯着看了好久"、我一句"公子想要下一任，问过我了吗"，把人堵得无话可说。没忍住笑了半声，立刻被抓了现行。争到最紧时，我伸手捏了捏闹得最凶的那张脸——高冷当场破功，另一个鼓着腮帮子不干了：主人偏心，我也要！`
  `（含AI生成内容 ｜ 角色二创自《凡人修仙传》，非商用同人；热搜仅作生活化转述，不介绍玩法、不引导参与）`
- **标签（12 个，第 1 个＝热搜标题原文）**：
  `发视频解锁你的下一任对象,下一任对象,韩老魔的宫装女友,宫装女友,凡人修仙传,幕沛灵,银月,吃醋,争宠,国风,AI短剧,甜宠`
- **封面**：`E13-下一任是谁合集\images\E13_幕沛灵银月_闺阁同框B_下_双图直传.png`（用户指定；biliup `--cover` 直接接受 PNG）

## 二、抖音（账号：creator）

- **标题**（≤30 字）：
  `热搜能解锁下一任对象？两位女友审问我`
- **简介**：
  `热搜说发视频能解锁下一任对象。银月举机献宝，幕沛灵当场按屏，两人一左一右逼问我想解锁谁。没忍住笑了半声被抓包，伸手捏了捏闹最凶的——另一个立刻炸毛：主人偏心，我也要！`
- **话题（编号列表，脚本解析格式；第 1 条＝热搜标题原文，硬上限 5 个）**：
  1. #发视频解锁你的下一任对象
  2. #韩老魔的宫装女友
  3. #凡人修仙传
  4. #争宠
  5. #AI短剧
- **封面**：同上 `E13_幕沛灵银月_闺阁同框B_下_双图直传.png`（sau `--thumbnail` 直接接受 PNG）
- **AI 自主声明**：`--declaration "内容由AI生成"`

> **短信风控门**：日志出现「📱 检测到短信验证码弹窗」后必须**硬暂停**等待用户提供 6 位验证码，拿码后**无 BOM** 写入 `D:\work\social-auto-upload\verify_code.txt`（用 `[System.IO.File]::WriteAllText`），再恢复流程。

> 发布统一走 `python d:\work\minimax_h3\tools\publish_episode.py --episode E13`（先 `--dry-run` 验证解析；发布素材路径以本文件为准则需同步确认 publish_episode.py 的 E13 路径映射）。
