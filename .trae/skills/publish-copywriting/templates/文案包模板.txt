# {编号} · {关键词} —— 双平台发布文案包

**成片**：`{分集目录}\video\{成片}.mp4`（时长、分辨率、音轨/BGM 情况）
**封面**：`{分集目录}\images\{封面}.png`（双平台同一张参考图，不从成片截帧）
**角色**：{角色与造型，一句话}
**热点**：{平台 + 热搜标题原文 + 热度；无热点写「无」，并注明第 1 话题改取本集主题}
**安全口径**：{二创/非商用、内容安全说明}

---

## 一、B 站（账号：{bilibili_main}）

- **分区**：`--tid 47`（动画·短片/同人）
- **版权**：自制 + 禁止转载
- **创作声明**：**含AI生成内容**（投稿时脚本一次带入，回读 decl_id=1 校验）
- **标题**（≤80 字，末尾带剧名+集号）：
  `{一句话核心看点}｜{剧名}{集号}`
- **简介**：
  `{剧情梗概，2—5 句，讲清人物、冲突、钩子}`
  `（含AI生成内容 ｜ 角色二创自《{原作}》，非商用同人）`
- **标签（10—12 个，逗号分隔；蹭热点时第 1 个＝热点原文）**：
  `{热点原文},{剧名},{IP},{角色},{品类},…`
- **封面**：`{封面路径，与头部一致}`

## 二、抖音（账号：{creator}）

- **标题**（≤30 字，纯口语钩子）：
  `{一句话钩子，不带剧名编号}`
- **简介**：
  `{一自然段口语化简介，前两句抓人}`
- **话题（编号列表，硬上限 5 个；第 1 个＝热点原文，无热点取本集主题）**：
  1. #{热点原文或本集主题}
  2. #{剧名}
  3. #{IP}
  4. #{角色或品类}
  5. #{品类}
- **封面**：同上 `{封面文件名}`
- **AI 自主声明**：`--declaration "内容由AI生成"`

> 发布统一走 `python d:\work\minimax_h3\tools\publish_episode.py --project {palace|dance} --episode {编号}`，先 `--dry-run` 校验解析。
